# Exemplo: código

Delegue a leitura inicial deste repositório à Gemma local (`delegate_file` / `delegate_batch`).

Retorne:

- arquitetura
- arquivos principais
- possíveis bugs
- pontos que precisam de revisão frontier

Não carregue o repositório inteiro no contexto da IA paga. Se o worker local falhar, continue sem ele.
